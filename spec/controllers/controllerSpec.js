"use strict";
import controller from "../../src/core/controller";

describe("test core controller", () => {
	// var controller = new controller();
	// var helper = require("../helper");

	// controller.setReqRes({}, helper.getRes())

	// it("funciton:sendSuccessMsg", () => {
	// 	spyOn(controller.res, "send");
	// 	controller.sendSuccessMsg();
	// 	expect(controller.res.send).toHaveBeenCalledWith({
	// 		success: true
	// 	})

	// 	controller.sendSuccessMsg([{title:1},{title:2}]);
	// 	expect(controller.res.send).toHaveBeenCalledWith({
	// 		success: true,
	// 		data: [{title:1},{title:2}]
	// 	})
	// });
});


